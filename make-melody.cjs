// Melodia instrumental original sintetizada, sem dependências.
const fs=require('fs');const rate=22050,seconds=48,n=rate*seconds,out=Buffer.alloc(44+n*2);out.write('RIFF');out.writeUInt32LE(36+n*2,4);out.write('WAVEfmt ',8);out.writeUInt32LE(16,16);out.writeUInt16LE(1,20);out.writeUInt16LE(1,22);out.writeUInt32LE(rate,24);out.writeUInt32LE(rate*2,28);out.writeUInt16LE(2,32);out.writeUInt16LE(16,34);out.write('data',36);out.writeUInt32LE(n*2,40);
const notes=[64,67,71,67,62,66,69,66,60,64,67,72,62,67,69,67,64,71,74,71,62,69,73,69,60,67,72,67,62,66,69,67];
const events=notes.map((note,i)=>({start:i*1.35,note,duration:4,volume:.20}));[48,43,45,41,48,43,41,43].forEach((note,i)=>events.push({start:i*5.4,note,duration:6,volume:.11}));
for(let i=0;i<n;i++){const t=i/rate;let v=0;for(const e of events){const age=t-e.start;if(age<0||age>e.duration)continue;const f=440*2**((e.note-69)/12);const env=Math.min(age/.025,1)*Math.exp(-age/1.5)*Math.min((e.duration-age)/.3,1);v+=e.volume*env*(Math.sin(2*Math.PI*f*age)+.28*Math.sin(2*Math.PI*f*2*age)+.1*Math.sin(2*Math.PI*f*3*age));}v*=Math.min(1,(seconds-t)/3);out.writeInt16LE(Math.max(-32767,Math.min(32767,Math.round(v*32767))),44+i*2);}fs.writeFileSync('assets/melodia.wav',out);console.log('48 segundos de melodia gerados.');
